---
description: Rolnopol Playwright framework — global conventions, coding standards, commits, execution, quality gates
alwaysApply: true
---

# Global guidelines — Rolnopol Playwright framework

This repo is a **Playwright + TypeScript** test automation framework for **Rolnopol** (external app under test). Deep reference: `@docs/test_framework_architecture_.md`, `@docs/rolnopol_context.md`, `@docs/coding-standards.md`.

## Boundaries

- Keep the framework **small and focused** — medium-light, recruitment-quality, not enterprise over-engineering.
- **Do not copy** Rolnopol application source code. Treat Rolnopol as an external system.
- App runs via Docker: `jaktestowac/rolnopol:1.25.0` on port 3000 (`docker compose up -d`).

## Stack and patterns

- **UI:** Page Object Model; selectors only in `src/pages/`.
- **API:** Request classes in `src/api/requests/`; assertions in `src/api/assertions/`.
- **Contracts:** OpenAPI at `OPENAPI_URL` is the source of truth for API response validation.
- **Data:** Static users in `src/test-data/`; factories in `src/factories/` for generated/invalid payloads.
- **Routes:** All paths in `src/routes/routes.ts`. API routes are relative to `/api/v1` (use with `API_BASE_URL` from `config/env.config.ts`).

## Environment

- No hardcoded URLs or credentials in tests — use `config/env.config.ts` and `.env` (from `.env-template`).
- OpenAPI URL is configurable via env.

## Coding standards (summary)

Full detail: `@docs/coding-standards.md`. UI/API specifics: `@ui-guideline`, `@api-guideline`.

- **Files:** kebab-case; suffixes `*.page.ts`, `*.request.ts`, `*.factory.ts`, `*.assertion.ts`, `*.mock.ts`, `*.fixtures.ts`, `*.spec.ts`, `*.setup.ts` — no `utils.ts`, `helpers.ts`, `test1.spec.ts`.
- **Naming:** `PascalCase` types; `camelCase` vars/methods (`verbNoun`); intent-clear names (not `res`, `x`, vague `data`).
- **Tests:** Arrange → Act → Assert; one main behavior; titles describe business behavior; tags `@ui`, `@api`, `@mock`, `@contract`, `@smoke`, `@business`, `@logged`, `@non-logged`.
- **TypeScript:** strict, no `any`; `unknown` then validate; explicit return types in `src/` and `config/` only.
- **Imports:** `@src/*`, `@config/*` — avoid deep `../../../`.
- **Hygiene:** no `test.only`, `console.log`, hard waits, unused code, TS/ESLint issues; tests independent of order; parallel execution (`fullyParallel: true`, multiple workers) — see `docs/known-issues.md`.

## Conventional commits

Based on [Conventional Commits Cheatsheet](https://gist.github.com/qoomon/5dfcdf8eec66a051ecd85625518cfd13). See `@docs/coding-standards.md` §28.

Format:

```txt
<type>(<optional scope>): <description>
```

**Description:** imperative present tense, lowercase, no trailing period (e.g. `add login page object`, not `Added login`).

**Types (pick first match):** `fix` (bug) → `feat` (behavior/UI/API change) → `test` → `style` → `docs` → `build` (deps, tooling) → `ops` (CI/CD, Docker) → `chore` (maintenance) → `perf` (performance) → else `refactor`.

**Scopes (optional, project areas — not issue IDs):** `ui`, `api`, `pages`, `config`, `e2e`, `ci`, `deps`, `docs`.

**Examples:**

```txt
test(ui): add positive login spec
feat(api): add OpenAPI response validation helper
fix(pages): use stable test id for marketplace buy button
docs: add coding standards
build(ci): add GitHub Actions workflow
chore: init
```

Breaking changes: `feat(api)!: ...` or footer `BREAKING CHANGE: ...`. Prefer small, logical commits.

## Rolnopol stability (read before running tests)

- JSON-file persistence, rate limiting, chaos engine, intentional bugs — prefer **stable** runs over speed.
- Config: Chromium only, `fullyParallel: true`, multiple workers, CI retries — see `playwright.config.ts` and `docs/known-issues.md`.

## Avoid at the start

Complex fixture chains, multiple browsers, random data everywhere, hard waits, `test.only`, unnecessary new dependencies.

## Running tests

```bash
npm run app:start    # if script exists
npm run app:wait     # wait for http://localhost:3000
npm run test         # full suite
npm run test:ui      # UI only
npm run test:api     # API only
```

Tags: `@smoke`, `@ui`, `@api`, `@contract`, `@mock`.

Flaky check (**recommended**, app must be running):

```bash
npm run test:flaky -- tests/ui/integration/login.spec.ts
```

- **Required:** user or agent passes **one spec file path** after `--`. Bare `npm run test:flaky` exits with an error and does **not** run the full suite.
- Runs `playwright test <path> --repeat-each=10 --retries=0` (run with `CI` unset when possible).
- Use a single new or changed spec file, not a directory.
- Document instability in `docs/testing-observations.md` instead of adding hard waits.

## Quality gates

- ESLint uses `eslint-plugin-prettier` so formatting is enforced in lint, not only via Prettier CLI.
- After general code changes: `npm run quality:quick` (lint, format check, typecheck).
- If `npm run lint` fails due to formatting: run `npm run format`, then re-run `npm run lint` and fix any remaining issues.
- Before considering work done: `npm run quality` (includes tests when available).

## After creating or changing tests

Agents **must** verify the change before marking the task done:

1. `npm run lint` — on formatting failures, run `npm run format` and re-run lint.
2. `npm run typecheck`
3. Run the affected test(s) with the app up (`npm run app:start`, `npm run app:wait`, then `npm test` or a targeted spec path) and confirm they pass.
4. **Recommended:** `npm run test:flaky -- <path-to-spec>` — path is mandatory; note flakiness in `docs/testing-observations.md`.

Pre-commit (`quality:quick`) does not run Playwright tests — run tests explicitly when working on specs.

## Agent roles

Before implementation, activate the relevant agent role rule. Each role defines workflow, responsibilities, and boundaries:

| Role | Rule | When to use |
|------|------|-------------|
| Test Discovery | `@agent-test-discovery` | Exploring a flow before coding |
| UI Implementation | `@agent-ui-implementation` | Implementing UI specs and Page Objects |
| API Contract | `@agent-api-contract` | Implementing API request classes and contract tests |
| Quality Gate | `@agent-quality-gate` | Running quality checks after each slice |

## Where to look

| Task | Doc / rule |
|------|------------|
| Implementation order, agent roles, vertical slices | `@docs/implementation-todo.md` |
| Naming, types, imports, commits (detail) | `@docs/coding-standards.md` |
| UI tests, POM, locators | `@ui-guideline` |
| API tests, OpenAPI | `@api-guideline` |
| Full structure & examples | `@docs/test_framework_architecture_.md` |
| App quirks (chaos, 429, MPA) | `@docs/rolnopol_context.md` |

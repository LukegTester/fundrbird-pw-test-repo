# URLs — copy to .env and adjust if the app runs elsewhere
BASE_URL=http://localhost:3000
API_BASE_URL=http://localhost:3000/api/v1
OPENAPI_URL=http://localhost:3000/schema/openapi.json

# Demo credentials are included here for reviewer convenience only.
# A real .env-template would leave USER_PASSWORD empty and document how to obtain secrets.
USER_EMAIL=demo@example.com
USER_PASSWORD=demo123
